
const driverPageStyles = theme => ({
	root: {
		flexGrow: 1,
		margin: theme.spacing.unit * 2,
		marginTop: theme.spacing.unit * 4
	},
	paper: {
		padding: theme.spacing.unit * 2,
		textAlign: 'center',
		color: theme.palette.text.secondary
	}
});

export default driverPageStyles;
